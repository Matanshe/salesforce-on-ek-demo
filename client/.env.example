# API Configuration
# For local development, use http://localhost:3000
# For production, use your deployed backend URL (e.g., https://your-app.herokuapp.com)
VITE_API_URL=http://localhost:3000

# API Security
# Generate a secure random secret key (32 bytes hex)
# Example: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
VITE_API_SECRET=your-secret-key-here

# Citation behavior: fullPage = navigate to article; modal = open citation in modal (e.g. for embedded agent)
# Omit or set to fullPage for current (default) behavior
VITE_CITATION_BEHAVIOR=fullPage

# Embed layout: when true, only the chat widget is shown (e.g. for static-site iframe). Use with VITE_CITATION_BEHAVIOR=modal
# URL override: ?embed=1 enables embed layout + modal citations without rebuilding
VITE_EMBED_LAYOUT=false
