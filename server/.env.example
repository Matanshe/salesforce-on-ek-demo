# =============================================================================
# Server environment variables
# Copy to server/.env and set values. Required for Salesforce OAuth and API auth.
# =============================================================================

# --- Salesforce OAuth (required for token + Agentforce) ---
# Login URL of your Salesforce org (e.g. https://yourorg.my.salesforce.com)
SALESFORCE_LOGIN_URL=

# Connected App credentials (Client Credentials flow).
# Create a Connected App in Salesforce and enable "Client Credentials Flow".
# If you use multi-customer (config/customers.json), you can leave clientId/clientSecret
# empty per customer and they will use these env vars instead.
CLIENT_ID=
CLIENT_SECRET=

# --- Agentforce (fallback when no customer selected) ---
# Agent ID from your Agentforce agent. Per-customer agents are in config/customers.json.
AGENTFORCE_AGENT_ID=

# --- API security (required) ---
# Secret used to sign client requests. Must match client VITE_API_SECRET.
# Example: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_SECRET=

# --- Optional ---
# Port (default 3000)
PORT=3000
# CORS allowed origin(s), comma-separated (default http://localhost:5173)
CORS_ORIGIN=http://localhost:5173
